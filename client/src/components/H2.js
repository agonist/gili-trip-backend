import React from "react";
import { Heading } from "evergreen-ui";

import { ITEM_SPACE } from "../constants";

const H2 = props => <Heading size={600} marginY={ITEM_SPACE} {...props} />;

export default H2;
