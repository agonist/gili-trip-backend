import React from "react";
import { Text } from "evergreen-ui";

const Small = props => (
  <Text
    fontSize={10}
    display="block"
    textTransform="uppercase"
    color="#66788A"
    {...props}
  />
);

export default Small;
